package com.likelion.week5.day221020.alphabet;

public class ConvertAlphabetToNum {

    // 알파벳 1글자를 받아서(parameter는 1개다)
    // A는 1 B는 2 .... Z는 26을 반환(return)하는 메소드를 만들어 보세요

    public static void main(String[] args) {
        String a = "A";

        // int convertAlphabetToNum(char c)
        // 이 기능은 왜 짜는 것일까?
        // 1. 메소드로 접근을 해야 한다.
        // 2. 무엇을 받고 무엇을 return할 것인가?
        // 3. loop가 들어간다고 하면 ~부터 ~까지가 들어가는 경우가 많다.
        // 4. 필요한 최소한의 기능만 넣는다.
        // 5. Scanner --> main에서만 / 메소드 안에서는 지양 '받는다'
        // 6. 받는다 != 입력받는다(method의 parameter)
    }



}
